jQuery.sap.declare("imed.app.consentforms.util.Formatter");
imed.app.consentforms.util.Formatter = {

		uppercaseFirstChar : function(sStr) {
			return sStr.charAt(0).toUpperCase() + sStr.slice(1);
		},
};